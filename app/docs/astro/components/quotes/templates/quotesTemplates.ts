import { quotesQuote} from "./quotesQuote";
import { quotesQuoteWSlider } from "./quotesQuoteWSlider";



/** All three examples in display order — pass directly to your examples loop. */
export const examples = [quotesQuote];
