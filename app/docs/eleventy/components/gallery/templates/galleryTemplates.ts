import { galleryMain } from "./galleryMain";
//import { galleryLinks } from "./galleryLinks";
//import { galleryStats } from "./galleryStats";


//export { galleryMain, galleryLinks, galleryStats };

/** All three examples in display order — pass directly to your examples loop. */
//export const examples = [galleryMain, galleryLinks, galleryStats];

export const examples = [galleryMain];
